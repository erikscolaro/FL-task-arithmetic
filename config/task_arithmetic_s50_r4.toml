# TaLoS configuration: sparsity=0.5, calibration_rounds=4

# Federated learning parameters
num-server-rounds = 30
num-classes-per-partition = 10
local-epochs = 8
fraction-train = 0.1

# Sparse fine-tuning with TaLoS
use-sparse-finetuning = true
sparsity-ratio = 0.5
num-calibration-rounds = 4

# Wandb configuration
group = "task-arithmetic-s50-r4-V2"
notes = "Task Arithmetic with 50% sparsity, 4 calibration rounds (standard)"
