# TaLoS configuration: sparsity=0.9, calibration_rounds=1

# Federated learning parameters
num-server-rounds = 30
num-classes-per-partition = 10
local-epochs = 8
fraction-train = 0.1

# Sparse fine-tuning with TaLoS
use-sparse-finetuning = true
sparsity-ratio = 0.9
num-calibration-rounds = 1

# Wandb configuration
group = "task-arithmetic-s90-r1"
notes = "Task Arithmetic with 90% sparsity (standard), 1 calibration round"
