# TaLoS configuration: sparsity=0.9, calibration_rounds=8

# Federated learning parameters
num-classes-per-partition = 10
local-epochs = 8
fraction-train = 0.1
fraction-evaluate = 1.0

# Sparse fine-tuning with TaLoS
use-sparse-finetuning = true
sparsity-ratio = 0.9
num-calibration-rounds = 8

# Wandb configuration
group = "task-arithmetic-s90-r8"
notes = "Task Arithmetic with 90% sparsity (standard), 8 calibration rounds (intensive)"
