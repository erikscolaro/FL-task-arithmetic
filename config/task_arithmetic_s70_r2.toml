# TaLoS configuration: sparsity=0.7, calibration_rounds=2

# Federated learning parameters
num-server-rounds = 30
num-classes-per-partition = 10
local-epochs = 8
fraction-train = 0.1

# Sparse fine-tuning with TaLoS
use-sparse-finetuning = true
sparsity-ratio = 0.7
num-calibration-rounds = 2
mask-calibration-type = 0

# Wandb configuration
group = "task-arithmetic-s70-r2"                                  #Davide is already running it
notes = "Task Arithmetic with 70% sparsity, 2 calibration rounds"
