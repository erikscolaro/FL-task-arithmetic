# TaLoS configuration: sparsity=0.95, calibration_rounds=8

# Federated learning parameters
num-server-rounds = 30
num-classes-per-partition = 10
local-epochs = 8
fraction-train = 0.1

# Sparse fine-tuning with TaLoS
use-sparse-finetuning = true
sparsity-ratio = 0.50
mask-calibration-type = 4

# Wandb configuration
group = "highest-magnitude-s50"
notes = "Highest magnitude masking with 50% sparsity"
