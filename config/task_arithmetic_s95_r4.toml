# TaLoS configuration: sparsity=0.95, calibration_rounds=4

# Federated learning parameters
num-server-rounds = 30
num-classes-per-partition = 10
local-epochs = 8
fraction-train = 0.1

# Sparse fine-tuning with TaLoS
use-sparse-finetuning = true
sparsity-ratio = 0.95
num-calibration-rounds = 4
mask-calibration-type = 0

# Wandb configuration
group = "task-arithmetic-s95-r4-V2"
notes = "Task Arithmetic with 95% sparsity (extreme), 4 calibration rounds (standard)"
